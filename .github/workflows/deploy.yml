name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies 📦
        run: npm ci

      - name: Print Project Structure 🔍
        run: |
          echo "Структура корня проекта:"
          ls -la
          echo "Структура директории src:"
          ls -la src
          echo "Структура директории секций src:"
          ls -la src/sections || echo "Директория секций не найдена"
          echo "Структура директории компонентов src:"
          ls -la src/components || echo "Директория компонентов не найдена"
          echo "Проверка содержимого vite.config.js:"
          cat vite.config.js

      - name: Print package.json scripts 📜
        run: |
          echo "Package scripts:"
          cat package.json | grep -A 10 "scripts"

      - name: Create .nojekyll file
        run: touch .nojekyll

      - name: Build 🔧
        run: npm run build
        env:
          NODE_ENV: production

      - name: Debug build output
        run: |
          echo "Поиск main.js в исходниках:"
          find src -name "main.js"
          echo "Директория сборки:"
          ls -la dist || echo "Директория dist не найдена"
          echo "Все JS файлы в сборке:"
          find dist -name "*.js" || echo "JS файлы не найдены"
          echo "Содержимое assets в dist:"
          ls -la dist/assets || echo "Директория assets не найдена"

      - name: Check build output 📊
        run: |
          echo "Директория сборки:"
          ls -la dist || echo "Директория dist не найдена"
          if [ -d "dist" ]; then
            echo "Содержимое директории dist:"
            ls -la dist
            echo "Проверка index.html в dist:"
            cat dist/index.html || echo "index.html не найден в dist"
          fi

      # Создание main.js если он отсутствует
      - name: Ensure main.js exists
  run: |
    mkdir -p dist/assets/js
    echo "console.log('Main.js loaded');" > dist/assets/js/main.js
    echo "Создан простой main.js"

      # Копирование статических ресурсов из public в dist
      - name: Copy public resources
        run: |
          if [ -d "public" ]; then
            echo "Копируем файлы из public в dist..."
            cp -R public/* dist/ || echo "Ошибка при копировании файлов"
          fi

      # Вручную копируем HTML-фрагменты при необходимости
      - name: Копирование HTML-файлов секций и компонентов 🔧
        run: |
          if [ -d "dist" ] && [ -d "src/sections" ]; then
            echo "Вручную копируем файлы HTML-секций..."
            mkdir -p dist/sections
            cp -R src/sections/* dist/sections/
          fi

          if [ -d "dist" ] && [ -d "src/components" ]; then
            echo "Вручную копируем файлы HTML-компонентов..."
            mkdir -p dist/components
            cp -R src/components/* dist/components/
          fi

      # Создаем .nojekyll в dist
      - name: Create .nojekyll in dist
        run: touch dist/.nojekyll

      # Создаем тестовую страницу
      - name: Create test page
        run: |
          echo '<!DOCTYPE html>
          <html>
          <head>
            <title>Test Page</title>
            <meta charset="UTF-8">
          </head>
          <body style="font-family: Arial, sans-serif; max-width: 800px; margin: 0 auto; padding: 20px;">
            <h1>Test Page for R36S_STORE_JS</h1>
            <p>If you see this, basic routing works!</p>
            <div>
              <h2>Debugging Info:</h2>
              <p>Deployment time: <span id="time"></span></p>
              <p>Base URL: <span id="baseUrl"></span></p>
              <p>Current path: <span id="path"></span></p>
            </div>
            <script>
              document.getElementById("time").textContent = new Date().toLocaleString();
              document.getElementById("baseUrl").textContent = window.location.origin + window.location.pathname.replace("test.html", "");
              document.getElementById("path").textContent = window.location.pathname;
            </script>
          </body>
          </html>' > dist/test.html

      - name: Final check
        run: |
          echo "===================="
          echo "FINAL BUILD STRUCTURE"
          echo "===================="
          find dist -type f | sort
          echo "===================="
          echo "INDEX.HTML CONTENT"
          echo "===================="
          cat dist/index.html
          echo "===================="

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist
          clean: true
          token: ${{ secrets.GITHUB_TOKEN }}
